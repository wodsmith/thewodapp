import { test } from "@playwright/test"

test.describe.skip("admin track management (e2e)", () => {
	test("placeholder", async () => {
		// e2e tests are executed via Playwright in CI environment
	})
})
