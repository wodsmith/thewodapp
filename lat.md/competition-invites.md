# Competition Invites

ADR-0011 ships a qualification-source → roster → invite-round system for championship competitions. Organizers declare where invitees come from, see a unified roster from those sources, and — later — send email-locked invites in waves.

Phase 1 is read-only: declarative sources, a roster table that joins source leaderboards with invite-state columns set to null, and the organizer route shell. Schema lives in [[apps/wodsmith-start/src/db/schemas/competition-invites.ts]]. ADR reference: `docs/adr/0011-competition-invites.md`.

Phase 2 (in progress) lays the data + logic groundwork for email-locked single-send invites: per-athlete invite rows in `competition_invites`, plaintext claim tokens (mirroring `team_invitations.token`), server-side issue + reissue helpers, and bespoke staging. Routes, Stripe workflow wiring, email queue, and the organizer UI follow in the remaining sub-arcs.

## Sources schema

A qualification source attached to a championship competition. Each row references either a single competition or a series (never both); allocation and division-mapping columns describe how many invitees the source contributes.

Defined by [[apps/wodsmith-start/src/db/schemas/competition-invites.ts#competitionInviteSourcesTable]] with three indexes — `(championshipCompetitionId, sortOrder)` drives the Sources tab list, `(sourceCompetitionId)` lets us find every championship a given source competition feeds, and `(sourceGroupId)` does the same for series. ULID primary keys generated via [[apps/wodsmith-start/src/db/schemas/common.ts#createCompetitionInviteSourceId]]. No FKs (PlanetScale convention).

## Sources helpers

Server-side CRUD helpers for the sources table live in [[apps/wodsmith-start/src/server/competition-invites/sources.ts]]. `listSourcesForChampionship`, `createSource`, `updateSource`, `deleteSource`, and `getSourceById` compose the CRUD surface.

The write-time constraint "exactly one of `sourceCompetitionId` / `sourceGroupId`" is enforced in [[apps/wodsmith-start/src/server/competition-invites/sources.ts#assertSourceReferenceValid]], not at the DB, because PlanetScale has no check constraints and a typed `InviteSourceValidationError` from one boundary is cleaner than mixing DB and validation errors. `updateSource` re-runs the validator against the post-patch shape so a partial update that breaks the rule fails before reaching the DB.

ADR-0012: `deleteSource` wraps the source delete and a cascade delete of the source's `competition_invite_source_division_allocations` rows in a single `db.transaction()` — there is no FK in the DB so the cascade is enforced in code.

## Allocations schema

ADR-0012's per-(source, championship-division) override table. Each row says "this source contributes exactly N qualifiers to this championship division"; absence of a row means "use the source default."

Defined by [[apps/wodsmith-start/src/db/schemas/competition-invites.ts#competitionInviteSourceDivisionAllocationsTable]]. ULID primary keys via [[apps/wodsmith-start/src/db/schemas/common.ts#createCompetitionInviteSourceDivisionAllocationId]] (`cisda_*` prefix). UNIQUE index on `(sourceId, championshipDivisionId)` enforces one allocation per pair so upsert semantics are unambiguous; a secondary index on `championshipDivisionId` powers the Sent-tab denominator query that sums across sources for one division. No FKs (PlanetScale convention) — references resolve to `competition_invite_sources.id` and `competition_divisions.id` of the championship in code.

## Allocations helper

[[apps/wodsmith-start/src/server/competition-invites/allocations.ts]] hosts the resolution math that turns "raw override rows + source defaults" into a per-(source, championship division) spot count. Pure where possible so callers can unit-test without DB.

[[apps/wodsmith-start/src/server/competition-invites/allocations.ts#resolveSourceAllocations]] is the pure function: given a source row, the championship's divisions, the source's allocation rows, and (for series sources) the comp count, it returns `{ total, byDivision }`. Algorithm: per championship division, use the override `spots` if a row exists; else use the source default — `globalSpots ?? 0` for single-comp, `(directSpotsPerComp ?? 0) * seriesCompCount + (globalSpots ?? 0)` for series, applied per division as the default. `0` overrides are honored ("this source gives this division nothing"); they do not collapse to the default.

[[apps/wodsmith-start/src/server/competition-invites/allocations.ts#listAllocationsForChampionship]] is the DB read: joins `competition_invite_source_division_allocations` with `competition_invite_sources` filtered by `championshipCompetitionId`, returning the raw allocation rows for every source attached to that championship in one query. Callers (the route loader, the roster computation, the claim guardrail) feed those rows into `resolveSourceAllocations`.

## Source server fns

The organizer endpoints live in [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts]] — `listInviteSourcesFn`, `createInviteSourceFn`, `updateInviteSourceFn`, `deleteInviteSourceFn` — gated by `MANAGE_COMPETITIONS` on the championship's organizing team.

`listInviteSourcesFn` returns the source rows plus resolved display lookups: `competitionNamesById` (for single-comp sources), `seriesNamesById` (for series sources), and `seriesCompCountsById` (number of competitions in each referenced series). The counts feed [[apps/wodsmith-start/src/components/organizer/invites/invite-sources-list.tsx]]'s `allocatedSpotsFor`, which computes a series' total contribution as `directSpotsPerComp * compCount + globalSpots` — `directSpotsPerComp` is per-comp so scaling by the series size matters for multi-comp series.

When a source references another competition or series, [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#requireSourcePermissions]] also requires `MANAGE_COMPETITIONS` on that source's organizing team. Per ADR Open Question 6 (same-org only for MVP) the two organizing teams must currently match; cross-org references throw. The policy lives in the server fn so a future phase can relax it by deleting a single check. Each fn wraps its handler in `withRequestContext` so `logEntityCreated` / `logEntityUpdated` / `logEntityDeleted` calls carry correlation IDs.

[[apps/wodsmith-start/src/server/competition-invites/sources.ts#deleteSource]] runs the allocation cascade and source delete inside a single tx, but pre-validates that the source row matches the supplied `championshipCompetitionId` before either delete fires. Without that pre-check a caller authorized for championship A could pass a `sourceId` belonging to championship B and wipe its allocation rows cross-tenant — the source-row delete is championship-scoped, but the allocations delete is keyed only by `sourceId`. If the pre-check fails the tx no-ops silently, matching the prior "delete returned 0 rows" behavior.

ADR-0012 added three more fns to the same module under the same `MANAGE_COMPETITIONS` gate. [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#getInviteSourceByIdFn]] is a single-source read used by the source details page loader; it resolves the championship organizing team from the source row so a typo'd `sourceId` cannot bypass the championship-side permission check. [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#listInviteSourceAllocationsFn]] returns three maps in one round-trip — `allocationsBySourceByDivision` (resolved per-(source, division) spots, defaults applied), `divisionAllocationTotals` (sum across sources for each championship division — drives the Sent-tab denominator), and `rawAllocationsBySource` (raw override rows grouped by source — lets the details page hydrate the "Use default" toggles for divisions that actually have overrides). [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#saveInviteSourceAllocationsFn]] persists the override table from the details page in a single `db.transaction`: each entry is an upsert when `spots` is a number (via MySQL's `onDuplicateKeyUpdate` keyed on the unique index) or a delete when `spots` is `null`.

## Source details page

ADR-0012 Phase 3: per-source details route at [[apps/wodsmith-start/src/routes/compete/organizer/$competitionId/invites/sources/$sourceId.tsx]] — hosts source meta editing plus the per-(championship division) allocation override table. The Sources tab's Edit button navigates here; the Add flow keeps the existing modal.

Two write surfaces. Source meta (kind, source comp/series, default spots, notes) calls [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#updateInviteSourceFn]] via the shared `InviteSourceForm` used by the Add modal. The per-division allocation table renders one row per championship division with a "Use default" checkbox and a numeric override input; saving calls [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#saveInviteSourceAllocationsFn]] with `null` for "use default" rows (deletes the override) and a number ≥ 0 for explicit overrides (upserts the row).

Loader gates on `MANAGE_COMPETITIONS` via [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#getInviteSourceByIdFn]] — a new single-source read that resolves the championship organizing team from the source row — and runs `Promise.all` for the source row, championship divisions, the championship-wide allocation map, and the source pickers. Discard restores the loader's seed values; Save invalidates the route on success.

## Roster computation

`getChampionshipRoster({ championshipId, divisionId })` in [[apps/wodsmith-start/src/server/competition-invites/roster.ts]] returns an ordered `RosterRow[]` for a championship + division. Invite-state columns are `null` in Phase 1.

The function loads all sources, delegates to [[apps/wodsmith-start/src/server/competition-leaderboard.ts#getCompetitionLeaderboard]] for single-comp sources and [[apps/wodsmith-start/src/server/series-leaderboard.ts#getSeriesLeaderboard]] for series sources, resolves each source's division mapping, and flags rows below each source's spot allocation as `belowCutoff`. Pure helpers (`parseDivisionMappings`, `resolveSourceDivisionId`, `resolveSpotsForDivision`, `aggregateQualifyingRows`) are extracted and unit-tested directly so the cutoff + skip-already-qualified logic can be exercised without mocking the leaderboard stack. The server fn `getChampionshipRosterFn` gates this with `MANAGE_COMPETITIONS` on the championship.

Series sources have two qualifier tiers handled by `loadSeriesDirectRows` and `loadSeriesGlobalRows`. Direct qualifiers come from each comp's own leaderboard — the source's `directSpotsPerComp` × number of comps in the series. Globals come from the series-aggregate leaderboard with direct qualifiers filtered out. The per-comp division for each comp is resolved via [[apps/wodsmith-start/src/db/schemas/series.ts#seriesDivisionMappingsTable]]; the resulting source cutoff expands to `directCount + globalSpots` so both tiers count as qualified for the `belowCutoff` waterline.

"Already qualified" in [[apps/wodsmith-start/src/server/competition-invites/roster.ts]] means fully qualified in a higher-priority source — being on a prior source's waitlist does not block the same athlete from qualifying in a later source. When a later source promotes a previously waitlisted athlete, the earlier waitlist row is removed so each athlete appears at most once (qualified rows win; waitlist rows dedupe across sources).

ADR-0012 Phase 6: per-leaderboard cutoffs in `getChampionshipRoster` no longer read source-level scalars directly. Instead the function loads the championship's allocation rows once via [[apps/wodsmith-start/src/server/competition-invites/allocations.ts#listAllocationsForChampionship]], computes a per-source resolved map via [[apps/wodsmith-start/src/server/competition-invites/allocations.ts#resolveSourceAllocations]] (cached per source), and uses the resolved value for `(sourceId, championshipDivisionId)` to truncate each `(sourceComp × sourceDivision)` leaderboard. The source's `globalSpots` / `directSpotsPerComp` act as defaults when no allocation row exists. Source-side divisions are mapped to championship divisions via the source's `divisionMappings` JSON, falling back to case-insensitive label match; an unmappable division returns `null` (no truncation) rather than risk dropping qualifiers. Series sources keep the full leaderboard for now — splitting per-comp direct vs. global tiers under per-division overrides is a follow-up TODO documented inline.

## Organizer route shell

The organizer route is [[apps/wodsmith-start/src/routes/compete/organizer/$competitionId/invites/index.tsx]]. It renders three tabs — Candidates, Sources, Sent — all live. Round History, Email Templates, and Series Global are deferred to later phases and intentionally omitted from the UI until they ship.

The loader loads sources, divisions, the first division's roster, the active-invites projection, and the audit-view all-invites projection in parallel via `Promise.all` and passes them to child components. The sidebar ([[apps/wodsmith-start/src/components/competition-sidebar.tsx]]) gains an "Invites" entry under the Athletes section. Series sources within the Sources tab render per-comp tabs plus a series-global tab via [[apps/wodsmith-start/src/components/organizer/invites/series-source-sub-tabs.tsx]] — Phase 1 is read-only, so the component receives data from the loader rather than fetching its own, and invite pills are deliberately omitted until Phase 2.

The original "Roster" tab was renamed to **Candidates** because the surface is a candidate-picking view (athletes pulled from sources plus bespoke drafts), not a registered roster. Internal helpers (`getChampionshipRoster`, `RosterRow`, `championship-roster-table.tsx`) keep their names — they describe the server-side join across source-competition leaderboards, which is still correct.

## Sent invites tab

The Sent tab is an audit view that groups every issued invite (active OR terminal) by championship division. Reads the `AuditInviteSummary` projection from `listAllInvitesFn`; no mutations live here.

The component is [[apps/wodsmith-start/src/components/organizer/invites/sent-invites-by-division.tsx#SentInvitesByDivision]] and the server fn is [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#listAllInvitesFn]]. `listAllInvitesFn` is intentionally distinct from `listActiveInvitesFn` rather than a widening of it: the Candidates tab's bespoke-draft list relies on the active filter to keep terminal history out of the staged-drafts list — widening the existing fn would push that filter onto every caller, which is exactly the foot-gun the active-marker pattern was designed to avoid.

The new fn returns an [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#AuditInviteSummary]] DTO that extends `ActiveInviteSummary` with `divisionLabel` (joined from `scalingLevels`), `sourcePlacementLabel` (denormalized "1st — Comp · Div" for source-origin rows), and `lastUpdatedAt` (mapped from the row's `updatedAt` — the table has no dedicated `lastSentAt` column today and `updatedAt` is the closest available signal of "last activity" because it bumps on every issue / reissue / dispatch / status flip).

The component renders one card per division using the loader's `championshipDivisions` ordering. Each card carries a status counter row computed from the *unfiltered* per-division set — counters tell you what's in the bucket; the page-level filters narrow the table inside. Page-level filters are status chips, origin chips, and a free-text name/email search. Per-division empty-state copy renders when filters narrow rows out, so the organizer always sees the full division layout. The single per-row action is "copy live claim link" for rows where `claimUrl !== null` — revoke / reissue / resend stay in the round builder (Phase 3 / sub-arc D).

ADR-0012 Phase 2: the per-division headline denominator (`divisions[].maxSpots` on the component prop) reads `divisionAllocationTotals[divisionId]` — the resolved sum of per-source invite allocations for that championship division — sourced from [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#listInviteSourceAllocationsFn]] in the route loader. It is no longer the championship's `competition_divisions.maxSpots` (registration capacity). The per-source breakdown chips read their `allocated` value from a loader-supplied `allocationsBySourceByDivision: Record<sourceId, Record<championshipDivisionId, number>>` map; the component no longer parses `competitionInviteSourcesTable.divisionMappings` JSON at render time. A division with a resolved total of `0` falls back to the "X accepted" rendering (no denominator), matching the prior optional-field signal.

## Feature-flag gate

Both the sidebar link and the route page are gated on the PostHog feature flag `competition-invites`. When disabled the link is hidden from [[apps/wodsmith-start/src/components/competition-sidebar.tsx]] and the route component redirects to the competition overview.

Pattern mirrors the existing `competition-global-leaderboard` gate in [[apps/wodsmith-start/src/routes/compete/organizer/series/$groupId/leaderboard.tsx]] — `posthog.isFeatureEnabled()` seeds state, `posthog.onFeatureFlags()` subscribes to flag updates. The gate only compares `=== true` / `=== false` so the `undefined` pre-load state neither shows the link nor triggers a redirect, avoiding flicker.

## Invites schema

Each invite is a per-athlete row in [[apps/wodsmith-start/src/db/schemas/competition-invites.ts#competitionInvitesTable]] carrying the email-locked claim token, status, and origin attribution. ULID primary keys via [[apps/wodsmith-start/src/db/schemas/common.ts#createCompetitionInviteId]].

The `activeMarker` column is the correctness pivot: it holds the literal `"active"` while `status IN (pending, accepted_paid)` and is NULL on every terminal state. Combined with the unique index `(championshipCompetitionId, email, championshipDivisionId, activeMarker)` this enforces "at most one active invite per (championship, division, email)" while letting declined/expired/revoked rows accumulate (MySQL treats multiple NULLs as distinct). The `claimToken` column is plaintext `varchar(255)` with its own unique index (`competition_invites_claim_token_idx`) — same shape as [[apps/wodsmith-start/src/db/schemas/teams.ts#teamInvitationTable]]'s `token` column, looked up directly via `eq()` rather than hashed. Terminal transitions null the token so historical rows coexist under the same multi-NULL semantics. `sendAttempt` (default 0) is the re-send counter that drives the Resend `Idempotency-Key` suffix; reusing `invite.id` across re-issues stays safe because the suffix rotates.

Draft bespoke rows (created before any send) use a distinctive shape: `activeMarker = "active"`, `claimToken = NULL`, `expiresAt = NULL`, `emailDeliveryStatus = "skipped"`. The unique-active index blocks duplicate bespoke adds for the same (championship, division, email). Secondary indexes cover the organizer table queries — `(roundId)`, `(sourceId)`, `(origin)`, `(status)`, `(email)`, plus `(championshipCompetitionId)` and `(userId)` for roster-side joins.

Phase 2 uses `roundId = ""` as a sentinel pending the Phase 3 `competition_invite_rounds` table; the column is NOT NULL with a default of `""` so a Phase-3 backfill can rewrite every Phase-2 row to a synthetic "Round 1 — Backfill" without schema gymnastics.

## Token helpers

Claim tokens are 32-byte URL-safe random strings stored plaintext in `competition_invites.claimToken`, mirroring the `team_invitations.token` pattern. Helpers live in [[apps/wodsmith-start/src/lib/competition-invites/tokens.ts]].

[[apps/wodsmith-start/src/lib/competition-invites/tokens.ts#generateInviteClaimTokenPlaintext]] uses `crypto.getRandomValues` + base64url-no-padding (via `@oslojs/encoding`) so every character carries entropy and the result is safe in a URL path. The returned string is written directly to the `claimToken` column at issue / reissue time and looked up directly via `eq(claimToken, ...)` on the way back in — no hashing, no last4, no multi-artifact wrapper. Pure function — safe to call from server functions, workflows, queue consumers, and tests.

## Token storage rationale

Plaintext storage matches [[apps/wodsmith-start/src/db/schemas/teams.ts#teamInvitationTable]] and the lookup at [[apps/wodsmith-start/src/server-fns/invite-fns.ts]] so the codebase has one invite-token pattern, not two.

Threat model: the token already lives forever in the recipient's email inbox and the email provider's outbound logs, so a DB hash was never the only or even primary copy of the secret. The actual auth gate is `identityMatch` — even with a stolen token, the visitor must be signed in as the invited email address (verification-gated sign-up if no account exists) to claim. The token is an unguessable identifier scoped to a recipient, not a bearer password. Defense-in-depth: terminal transitions (`accepted_paid`, `declined`, `revoked`, `expired`) null `claimToken` so a leaked active row cannot replay after payment, and the organizer can copy the live claim URL straight from the invite list UI to resend or hand-deliver — the user-visible feature that motivated the change.

## Issue helpers

The DB-side-only layer that writes invite rows lives in [[apps/wodsmith-start/src/server/competition-invites/issue.ts]]. It never renders HTML and never enqueues — that belongs to the `issueInvitesFn` server fn in a later sub-arc.

[[apps/wodsmith-start/src/server/competition-invites/issue.ts#issueInvitesForRecipients]] runs in a single transaction: it snapshots existing active invites for the recipient emails, treats matches as `alreadyActive` (annotated with `isDraft` when the row has no `claimToken` yet so the caller can choose to reissue instead of skip), and inserts fresh rows for the rest. Each new row carries a freshly generated plaintext token, `activeMarker = "active"`, `status = "pending"`, and `sendAttempt = 0`. The return shape `{ inserted, alreadyActive }` hands the caller the inserted rows including their `claimToken` so the send pipeline can build claim URLs without re-querying.

[[apps/wodsmith-start/src/server/competition-invites/issue.ts#reissueInvite]] rotates the token on an existing row in place, increments `sendAttempt`, bumps `expiresAt`, restores `activeMarker = "active"`, and flips `expired` rows back to `pending`. It covers both the "extend an expired invite" path and the "activate a draft bespoke invite" path (draft rows have `claimToken = NULL`; reissue installs the first token). Preserving `invite.id` across rotations keeps Stripe metadata, webhook correlation, and audit queries stable. Both helpers reject via [[apps/wodsmith-start/src/server/competition-invites/issue.ts#FreeCompetitionNotEligibleError]] when the target division resolves to a $0 registration fee — free competitions are scope-trimmed out of invites in the MVP (ADR-0011 "Capacity Math → Free competitions").

[[apps/wodsmith-start/src/server/competition-invites/issue.ts#normalizeInviteEmail]] (trim + lowercase) is the canonical normalizer — applied at every write, every lookup, and every identity match so the email lock compares apples to apples.

## Bespoke helpers

Bespoke staging lives in [[apps/wodsmith-start/src/server/competition-invites/bespoke.ts]]. A draft bespoke invite stages a row the organizer can later include in a send; it has the distinctive "active but `claimToken = NULL`" shape described in "Invites schema".

[[apps/wodsmith-start/src/server/competition-invites/bespoke.ts#createBespokeInvite]] validates the email format, rejects free divisions, checks the unique-active invariant, and inserts a draft row. [[apps/wodsmith-start/src/server/competition-invites/bespoke.ts#createBespokeInvitesBulk]] accepts a paste body — CSV, TSV (Google Sheets paste), or one-email-per-line — parses rows via [[apps/wodsmith-start/src/server/competition-invites/bespoke.ts#parseBespokePaste]], normalizes emails, dedups within-batch, checks cross-batch against existing active invites, and inserts the survivors. Duplicates and invalid rows come back as `BulkDuplicateRow[]` / `BulkInvalidRow[]` — structured results, not exceptions — so the organizer UI can render row-level inline feedback. The paste is capped at `BESPOKE_BULK_MAX_ROWS` (500, per ADR-0011 OQ#8).

The line parser auto-detects delimiter per line — tab wins when present so a Google Sheets column paste keeps comma-containing fields intact — and skips literal header rows whose first column is `email` (case-insensitive). A division-hint column is accepted for copy-paste compatibility but ignored in Phase 2 (the caller always passes `championshipDivisionId` explicitly).

## Claim resolution

Read-side helpers for the `/compete/$slug/claim/$token` route live in [[apps/wodsmith-start/src/server/competition-invites/claim.ts]] (DB reads) and [[apps/wodsmith-start/src/server/competition-invites/identity.ts]] (pure helpers route files can import without dragging `getDb` into the client bundle).

The split exists because Vite chokes on `cloudflare:workers` when bundling client code, and `getDb` transitively imports it. Route files import only from `identity.ts`; server-side callers can use either module.

[[apps/wodsmith-start/src/server/competition-invites/claim.ts#resolveInviteByToken]] looks up the row directly via `eq(competitionInvitesTable.claimToken, token)` — same pattern as `[[apps/wodsmith-start/src/server-fns/invite-fns.ts]]` for team invites. It returns `null` on miss so the route can render the generic "invalid link" page without leaking whether the token ever existed. [[apps/wodsmith-start/src/server/competition-invites/identity.ts#assertInviteClaimable]] throws [[apps/wodsmith-start/src/server/competition-invites/identity.ts#InviteNotClaimableError]] when status, `claimToken`, `expiresAt`, or `activeMarker` fail the live checks — each terminal state maps to a distinct `reason` (`expired`, `declined`, `revoked`, `already_paid`, `not_found`) so the route can render tailored copy.

[[apps/wodsmith-start/src/server/competition-invites/identity.ts#identityMatch]] is the pure email-lock gate. Given `{ email }` from the session (or `null`) plus the invite plus an `accountExistsForInviteEmail` boolean (resolved by the caller against `userTable`), it returns the discriminated result the route loader branches on: `{ ok: true }`, `{ ok: false, reason: "wrong_account" | "needs_sign_in" | "needs_sign_up" }`. Case-insensitive on both sides so `Mike@Example.com` and `mike@example.com` compare equal.

[[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#getInviteByTokenFn]] also cross-checks `competition_registrations` when the visitor is signed in as the invited identity: a non-removed row for `(eventId, userId, divisionId)` short-circuits to `not_claimable` with reason `already_paid`. This is the correct defense because it works regardless of which lane (public, organizer-manual, or prior invite claim) created the registration — a still-`pending` invite for an already-registered athlete should not re-enter the payment flow. **This cross-check only fires when the token still resolves**: it sits inside `getInviteByTokenFn` after `resolveInviteByToken`, so it covers stale-but-still-`pending` invites, not paid-then-replayed links.

For the replay-after-paid case, the Stripe workflow nulls `claimToken` on `pending → accepted_paid` (so the same email link can't be used to mint a second registration). `resolveInviteByToken` then returns `null` and `getInviteByTokenFn` returns `not_found`. The route loader at [[apps/wodsmith-start/src/routes/compete/$slug/claim/$token.tsx]] handles this by reading the parent route's `userRegistrations` loader data when `reason === "not_found"` and a session exists: a matching active registration triggers the friendly `already_claimed` branch instead of the generic invalid-link error. This keeps the server fn's response surface narrow while letting the route own the "you already registered" recovery UX.

The `already_paid` reason is treated as a soft outcome by the route, not an error: the loader `redirect`s to `/compete/$slug/registered` instead of rendering a destructive alert. Already-registered athletes aren't in an error state — they just landed on the wrong page.


## Claim allocation guardrail

ADR-0012 Phase 5 closes the athlete-side loop on per-(source, division) allocation: source-attributed invites that would push the bucket over its cap are blocked with a new `over_allocated` reason. Bespoke invites bypass.

Authoritative gate at the Stripe workflow's `create-registration` step in [[apps/wodsmith-start/src/workflows/stripe-checkout-workflow.ts]] — same fail-and-refund pattern division capacity uses. Claim-time check is best-effort UX: it stops the obvious "spot was filled an hour ago" case from reaching Stripe Checkout but doesn't pretend to prevent the genuine race.

Counts are consumption-of-a-slot only: `competition_invites.status = "accepted_paid"` (Stripe-in-flight stays `pending`). Bucket key is `(sourceId, championshipDivisionId)`. `allocation === 0` is treated as "no cap configured" rather than "cap is zero" — over-issued invites still claim, matching the Sent tab's "X accepted" fallback.

Pure check: [[apps/wodsmith-start/src/server/competition-invites/identity.ts#assertInviteWithinAllocation]]. DB reads: [[apps/wodsmith-start/src/server/competition-invites/claim.ts#getAcceptedPaidCountForBucket]] and [[apps/wodsmith-start/src/server/competition-invites/claim.ts#resolveAllocationForInvite]] (override → source default fallback). Wired into [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#getInviteByTokenFn]] after the `already_paid` cross-check and before identity match. The route at [[apps/wodsmith-start/src/routes/compete/$slug/claim/$token.tsx]] renders an info-style "This division has filled its spots from this qualifier" page on `over_allocated` — soft outcome, not destructive error.


## Claim routes

The athlete-facing surfaces live under `apps/wodsmith-start/src/routes/compete/$slug/claim/`.

Phase 2 sub-arc B wires three pages: [[apps/wodsmith-start/src/routes/compete/$slug/claim/$token.tsx|$token.tsx]] (claim landing), [[apps/wodsmith-start/src/routes/compete/$slug/claim/$token/decline.tsx|$token/decline.tsx]] (explicit decline), and a sibling [[apps/wodsmith-start/src/routes/compete/$slug/invite-pending.tsx|invite-pending.tsx]] page for wrong-account recovery.

`$token.tsx` is the entry point clicked from the email. The loader calls [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#getInviteByTokenFn]] — a public (session-free) server fn that resolves the token, confirms the invite's championship slug matches the URL (anti-typo guard), and returns either `{ kind: "not_claimable", reason }` or `{ kind: "claimable", invite, championshipName, divisionLabel, accountExistsForInviteEmail }`. The loader then runs `identityMatch` against `context.session`. The four-way branch is: happy-path → render pre-attached registration CTA; `wrong_account` → render the log-out page (see below); `needs_sign_in` / `needs_sign_up` → `redirect` into `/sign-in?redirect=…&email=<invite.email>&invite=<token>` (or sign-up equivalent) so post-auth re-entry lands back on the claim page with a session that matches.

The `wrong_account` branch is intentionally minimal — it's a single **Log out** button. Clicking it calls [[apps/wodsmith-start/src/server-fns/auth-fns.ts#logoutFn]] (which invalidates the KV session and clears the session + active-team cookies), then `window.location` to `/sign-in?redirect=/compete/$slug/claim/$token&email=<invite.email>&invite=<token>` (or `/sign-up` when `accountExistsForInviteEmail` is false). Anything more elaborate is dead UI: the visitor must end up unauthenticated before re-entry, and we already know exactly which email + redirect they need.

The happy-path CTA links to `/compete/$slug/register?divisionId=<invite.championshipDivisionId>`. The register route's search schema accepts `divisionId` (and a Phase 2D `invite=<token>`). Presence of `divisionId` routes the athlete to [[apps/wodsmith-start/src/components/registration/registration-form.tsx#InviteRegistrationForm]] instead of [[apps/wodsmith-start/src/components/registration/registration-form.tsx#PublicRegistrationForm]] — explicit variants rather than mode booleans. Both variants share state through [[apps/wodsmith-start/src/components/registration/use-registration-form.ts#useRegistrationForm]] and compose the same section components ([[apps/wodsmith-start/src/components/registration/registration-sections.tsx#AffiliateSection]], etc.).

The invite variant renders [[apps/wodsmith-start/src/components/registration/invite-division-hero.tsx#InviteDivisionHero]] in place of the picker, pins the invited division as the only selection, and bypasses the public registration window — the invitation itself is the authorization to register. If the URL points at a division that's no longer eligible (full, already-registered, removed) the variant transparently falls back to the public flow so the athlete can still pick something else rather than seeing nothing.

`$token/decline.tsx` uses the same loader branches, then POSTs through [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#declineInviteFn]] on confirmation. The server fn re-runs resolve + identity-match (server is the authority — a forged POST can't bypass email-lock) before calling [[apps/wodsmith-start/src/server/competition-invites/decline.ts#declineInvite]], which transitions `pending → declined` and nulls `activeMarker` + `claimToken` so the link dies immediately. Declines are idempotent at the DB level: a zero-affected-rows outcome re-reads to tell "already terminal" apart from "row missing".

`invite-pending.tsx` is a fallback landing for visitors who arrive without a token (e.g. a stale bookmark) — it tells them to re-open the email rather than guessing a URL. The claim route's `wrong_account` branch no longer routes through it; the post-logout redirect goes straight to `/sign-in` with the original claim URL pinned as `redirect`.

## Email delivery

Invite emails ride the existing broadcast queue binding (`BROADCAST_EMAIL_QUEUE`) with a discriminator.

[[apps/wodsmith-start/src/server/broadcast-queue-consumer.ts]] exports a union `QueueEmailMessage = BroadcastEmailMessage | InviteEmailMessage` and dispatches per-message on the `kind` field. Broadcast messages without `kind` stay backward-compatible.

[[apps/wodsmith-start/src/server/broadcast-queue-consumer.ts#InviteEmailMessage]] carries one invite per message (HTML is pre-rendered per-recipient because each claim URL is unique). The consumer calls Resend with `Idempotency-Key: invite-<inviteId>-<sendAttempt>` so re-sends after an extend/reissue actually dispatch — the suffix rotates on each attempt, preventing Resend from silently deduplicating. Delivery outcome flips `competition_invites.emailDeliveryStatus` to `sent` / `failed` (with `emailLastError` captured). The message is acked on failure to avoid duplicate sends on transient 5xx retries — organizers see the failure state on the roster and choose to re-send.

In dev (no `RESEND_API_KEY`) the consumer logs the preview and marks delivery as `skipped` so seeded + test flows don't block on external HTTP.

[[apps/wodsmith-start/src/react-email/competition-invites/invite-email.tsx]] renders the branded email: hero/headline, division + source + deadline card, primary claim CTA, and a secondary decline link. The subject + body text are organizer-supplied at send time (Phase 2 uses a single default; Phase 4 introduces templates).

## Send pipeline

[[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#issueInvitesFn]] is the organizer's send button. Permission: `MANAGE_COMPETITIONS` on the championship's organizing team. Steps:

1. Resolves the championship, division, and organizing team display data.
2. Parses `rsvpDeadlineDate` (a `YYYY-MM-DD` calendar string from the dialog) into a UTC end-of-day `expiresAt` and a hand-formatted month/day/year label. Sending the deadline as a `Date` would parse local-tz on the client and re-format on the worker (TZ=UTC), shifting the email's "Respond by" line by one calendar day for every organizer west of UTC.
3. Calls [[apps/wodsmith-start/src/server/competition-invites/issue.ts#issueInvitesForRecipients]] to insert new rows and detect `alreadyActive` overlaps. Free divisions are rejected here (ADR-0011 "Capacity Math"). `alreadyActive`'s `isDraft` flag flips true for `pending` rows that either lack a token (a bespoke staged-but-unsent row) *or* have `emailDeliveryStatus = "failed"` from a prior dispatch crash — both branches feed the recovery path below so an interrupted send self-heals.
4. For each `alreadyActive` row flagged `isDraft: true` calls [[apps/wodsmith-start/src/server/competition-invites/issue.ts#reissueInvite]] to activate / re-deliver — rotates in a fresh token, sets `expiresAt`, bumps `sendAttempt`. **Known gap (regression-guarded, not yet fixed):** this loop is *not* wrapped in try/catch the way step 5 is. If `reissueInvite` throws on the Nth draft, the first N-1 drafts have rotated tokens and live state, but the request aborts before the dispatch loop runs, so neither the activated drafts nor any of the freshly-inserted rows from step 3 receive an email. A retry can't recover them — they come back as `alreadyActive` non-drafts (skipped). The fix is symmetric per-iteration try/catch that pushes onto the same `failed[]` array step 5 uses; the test [[apps/wodsmith-start/test/server-fns/competition-invite-fns.issue.test.ts]] (regression-guard case) locks in the current behavior so a future fix updates the assertion alongside the code.
5. Renders email HTML per invite via [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts]]'s `renderInviteEmailHtml` — claim URL is built from the row's stored `claimToken` value (same source the organizer invite list reads from for resend / copy-link), and the decline URL is `<claim>/decline`. Each iteration is wrapped in try/catch: on throw the row's `emailDeliveryStatus` is flipped to `failed` with `emailLastError` populated, so a re-click of Send picks it up via the `isDraft`-on-failed branch above. Without this the row would commit in `queued` with no queue message, fall into the non-draft `alreadyActive` skip bucket, and the unique-active index would block any fresh invite for the same (championship, email, division) until the expiry sweep runs days later.
6. Enqueues `InviteEmailMessage` per invite onto `BROADCAST_EMAIL_QUEUE`. In dev without the binding, logs instead of sending.
7. Returns `{ sentCount, skipped, failed }` where `skipped` is non-draft `alreadyActive` rows and `failed` is per-recipient render/enqueue errors the dialog renders inline so the organizer sees a partial-success outcome instead of a blanket "Send failed".

[[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#createBespokeInviteFn]] and [[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#createBespokeInvitesBulkFn]] wrap the helpers from [[apps/wodsmith-start/src/server/competition-invites/bespoke.ts]] with the standard permission gate and logging. Bulk returns `{ created, duplicates, invalid }` so the UI can surface row-level feedback.

Server-fn coverage lives in six sibling test files under `apps/wodsmith-start/test/server-fns/`: `competition-invite-fns.athlete.test.ts` (athlete-facing token resolution + decline, including email case-insensitivity and the registration short-circuit), `competition-invite-fns.issue.test.ts` (send pipeline including the activation-loop regression guard above), `competition-invite-fns.bespoke.test.ts` (paste-text limits, email validation), `competition-invite-fns.lists.test.ts` (claim URL projection from token + slug, audit-tab division-label fallback), `competition-invite-fns.allocations.test.ts` (resolved-spots aggregation, transactional upsert/delete distinction between `null` and `0`), and `competition-invite-fns.sources.test.ts` (cross-org rejection + cross-championship tampering on update).

## Registration hand-off + Stripe workflow

[[apps/wodsmith-start/src/server-fns/registration-fns.ts]]'s `initiateRegistrationPaymentFn` accepts an optional `inviteToken`. When supplied:

- The token is resolved + claimability-checked + email-matched against the session — any of these failing short-circuits with an error that the registration form can surface.
- The submitted `items` must be exactly the invited division — `items.length === 1 && items[0].divisionId === invite.championshipDivisionId`. Invites are pinned per division, so the server rejects both wrong-division submissions and "invited division plus extras". The registration form locks the UI to match (single read-only division, no multi-select) when `inviteToken` is present.
- The registration-window check is skipped for invite-holders (invites often precede public-open or survive past close). The form mirrors this — the `!registrationOpen` disable is bypassed for both the submit button and the form inputs (division select, affiliate, team fields, waiver toggles, question fields) when the invite lock is active. Otherwise the submit unlocks but the required fields stay frozen, and the invitee can't actually fill anything in.
- The single item's purchase metadata gets `inviteId` tagged so the Stripe workflow can flip the invite on payment success.

[[apps/wodsmith-start/src/workflows/stripe-checkout-workflow.ts]] reads `inviteId` from the purchase metadata inside `create-registration`, threads it through `RegistrationStepResult`, and runs a new `update-competition-invite-status` step after registration creation. The step flips the invite to `accepted_paid`, sets `paidAt`, sets `claimedRegistrationId`, and nulls `claimToken` so a replay of the original email link short-circuits. Idempotent via the `status = "pending"` guard on the update predicate so a workflow retry doesn't double-flip. `processCheckoutInline` (local-dev path) runs the same helper to keep behavior consistent without Cloudflare Workflows.

When a 100%-off coupon zeroes the total, the registration server fn takes the `allFree` branch and skips Stripe entirely — so it mirrors the workflow flip inline right after creating the registration row. Without this, an invite-locked free checkout would leave the invite stuck in `pending` even though the athlete is registered.

## Organizer send UX

Phase 2D makes the organizer invite buttons clickable. See [[apps/wodsmith-start/src/routes/compete/organizer/$competitionId/invites/index.tsx]] for the shell.

Three dialogs sit in [[apps/wodsmith-start/src/components/organizer/invites/add-bespoke-invitee-dialog.tsx]], [[apps/wodsmith-start/src/components/organizer/invites/bulk-add-invitees-dialog.tsx]], and [[apps/wodsmith-start/src/components/organizer/invites/send-invites-dialog.tsx]]. Add/Bulk dialogs wrap the bespoke staging server fns; Send wraps `issueInvitesFn`. The route header carries the three buttons (Add invitee / Bulk add / Send invites N); Send is disabled when no recipients are selected. The Send dialog computes its defaults locally from the championship + division props passed by the parent route — subject defaults to `You're invited to {championshipName} - {divisionLabel}` and the body textarea defaults to a generic invitation message — so organizers don't have to retype either on every send. The greeting line (`Hey {athleteName},`) is rendered automatically per-recipient by [[apps/wodsmith-start/src/react-email/competition-invites/invite-email.tsx#CompetitionInviteEmail]] above the body, so the textarea holds the message-only portion.

When the candidates table is filtered to a single source division, the parent route resolves the matching championship division by label and passes it as `defaultDivisionId` to [[apps/wodsmith-start/src/components/organizer/invites/send-invites-dialog.tsx]] so the dialog opens already pointed at the same division — saves the organizer a click and avoids the "filtered Rx Men candidates, dialog defaulted to Scaled" footgun. The match is label-based (case- and whitespace-insensitive) since source-division and championship-division ids live in different scaling groups; if no label matches, the dialog falls back to the first championship division.

The bulk dialog only exposes an "emails — one per line or comma-separated" affordance even though the underlying `parseBespokePaste` helper still understands the richer CSV/TSV row shape. Before sending, it splits the textarea on `[\n,]+` and rejoins with newlines so a single comma-separated line becomes one email per row (otherwise the helper's CSV parser would treat trailing emails as name fields). The primary CTA reads "Invite", closes the dialog on success, and surfaces the created/duplicate/invalid counts via a sonner toast — `toast.warning` when there are duplicates or invalid lines, `toast.success` otherwise.

[[apps/wodsmith-start/src/components/organizer/invites/championship-roster-table.tsx]] now accepts optional `selectedKeys` / `onToggleSelection` / `onToggleAll` props. When the parent supplies them a checkbox column appears; rows with no `athleteEmail` are disabled with a "No email on file" tooltip so the organizer can't try to invite an athlete without a resolvable address. The table also takes an optional `isRowAlreadyInvited(row)` callback — when it returns true the row's status pill renders "Invited" instead of "Not invited", the checkbox is disabled with an "Already invited" tooltip, and the row is excluded from select-all so the "Send invites (N)" chip can't silently disagree with the visible checked-box count. [[apps/wodsmith-start/src/server/competition-invites/roster.ts#RosterRow]] now carries `athleteEmail` — hydrated at the end of `getChampionshipRoster` via a single bulk `userTable` lookup so the Send dialog has an email to put on each recipient.

[[apps/wodsmith-start/src/server-fns/competition-invite-fns.ts#listActiveInvitesFn]] returns an `ActiveInviteSummary` projection that includes `claimToken` so the organizer UI can render the full claim URL inline (copy-to-clipboard for resend / hand-delivery is the user-visible motivator for plaintext storage). The same payload still carries a `hasClaimToken` boolean so the draft-vs-already-sent distinction is a single field check rather than an emptiness test.

The draft-bespoke section renders inline below the roster table, pulling from `listActiveInvitesFn`. Checked draft rows flow into the recipient list alongside selected source rows; the Send dialog shows a union of both with a recipient-count chip. After a successful send the route invalidates so the just-activated bespoke rows fall off the draft list (they now have tokens).

ADR-0012 Phase 4 surfaces per-(source, championship-division) allocation in the Candidates tab and the Send dialog. [[apps/wodsmith-start/src/components/organizer/invites/championship-roster-table.tsx]] accepts `allocationsBySourceByDivision` + `championshipDivisions` and renders a compact allocation strip above the table. The strip leads with a `Qualifies N · M/T sources` summary (total qualifying spots, count of allocated source-division groups vs. total) and one chip per allocated `(sourceId, sourceDivisionLabel)` group present in the visible rows; the `→ {championshipDivisionLabel}` segment is only rendered when source and target labels differ (so same-label cases collapse to just `{sourceDivisionLabel} · Top N`), removing the `"X → X"` redundancy the original banner had. Source-division groups with no matching championship division (or a resolved `0`) are tucked behind a single amber `N unallocated` popover trigger that lists them grouped by source competition — keeping the noisy "No allocation" rows out of the primary scan path while leaving them one click away. Same case-insensitive label match the route uses for the Send dialog's `defaultDivisionId`. [[apps/wodsmith-start/src/components/organizer/invites/send-invites-dialog.tsx]] now accepts `allocationsBySourceByDivision`, `existingActiveCountsBySourceByDivision`, and `sourceLabelsById`. The dialog buckets the staged recipients by `(sourceId, targetDivisionId)`, renders an inline per-bucket breakdown ("`{sourceLabel}` → `{divisionLabel}`: spot N of M") in the recipients panel, and surfaces a non-blocking amber alert listing every bucket where `existing + incoming > allocation` (Round 1 over-invites are allowed by design; the warning is informational and the Send button stays enabled). Bespoke recipients (`sourceId === null`) are excluded from the bucket math entirely. The route loader computes `existingActiveCountsBySourceByDivision` from the audit projection (`activeMarker === "active"` filtered to source-origin rows) and passes it to the dialog alongside the resolved allocation map and a `sourceLabelsById` derived via `resolveSourceLabel`. Both the table and the dialog short-circuit gracefully when the new props are absent — older callers and the existing test fixtures don't have to change.

## Registration hand-off from claim

The claim route's "Continue to registration" CTA now links to `/compete/$slug/register?invite=<token>&divisionId=<id>`.

The register route's `validateSearch` accepts both `invite` (the plaintext token) and `divisionId` (the invited division — invites are locked to one division at issue time). The route forwards both into `RegistrationForm` as the `inviteToken` and `inviteDivisionId` props. When both are present and the division resolves against the competition's scaling group, the form treats the registration as invite-locked: it pre-seeds `selectedDivisionIds = [inviteDivisionId]`, seeds the team-entry map for team divisions, and replaces the multi-select dropdown with a read-only "Invited Division" display. The closed-window banner and submit-button "Registration Closed" copy are also bypassed in this mode so the registration-window-skip from the server fn isn't visually contradicted. Combined with the purchase-metadata + Stripe-workflow wiring from sub-arc C, this closes the Organizer Send → athlete claim → Stripe → `accepted_paid` loop end-to-end.

## Prior team prefill

When an athlete arrives at the registration form via an invite for a team division, the form pre-fills the team name and teammate slots from the team they were on in the qualifying source competition.

The register route loader, on receiving `?invite=<token>`, calls a local `getInvitePrefillFn` server function that resolves the token (re-using [[apps/wodsmith-start/src/server/competition-invites/claim.ts#resolveInviteByToken]]), authorizes the caller by requiring the session user's normalized email to match the invite's normalized email (the prefill payload exposes other people's emails and names, so the token alone is not enough — `getInviteByTokenFn`'s public lookup only returns the invited identity's own metadata), verifies the URL slug against the invite's championship to guard against pasted-in-wrong-tab errors, and delegates to [[apps/wodsmith-start/src/server/competition-invites/prior-team.ts#getPriorTeamForInvite]]. That helper uses `invite.sourceCompetitionId` to find the invitee's `competition_registrations` row in the prior comp; when `athleteTeamId` is set, it reads the prior `teamName` and joins `team_memberships` → `users` to project `{email, firstName, lastName, affiliateName}` for every other active member. The captain (= invitee) is filtered out via `ne(userId, inviteeUserId)`. Bespoke invites (`sourceCompetitionId IS NULL`) and individual prior registrations (`athleteTeamId IS NULL`) return `null` and the form falls back to empty teammate slots. When `invite.userId` isn't denormalized (older invites issued pre sign-up), the helper resolves the user by normalized email; if no account exists yet, it returns `null` rather than guessing.

The loader threads `invitePriorTeam` into [[apps/wodsmith-start/src/components/registration/registration-form.tsx#InviteRegistrationForm]] as `prefillTeammates` + `prefillTeamName`, which forward into [[apps/wodsmith-start/src/components/registration/use-registration-form.ts#useRegistrationForm]]'s `teamEntries` initial state. Slots are hydrated up to `championshipDivision.teamSize - 1`: a larger prior team is truncated, a smaller one pads with empty rows. All slots remain editable so the invitee can swap teammates. The fallback path in `InviteRegistrationForm` that bounces ineligible-division invites to `PublicRegistrationForm` strips the prefill props from the spread so the public flow doesn't carry stale invite-only state.

## Invite expiry sweep

[[apps/wodsmith-start/src/server/competition-invites/expiry.ts#sweepExpiredInvites]] paginates expired pending invites and flips them to `expired` in batches.

On each row it nulls `activeMarker` + `claimToken` so the link dies immediately and the unique-active index unblocks a future re-invite.

Idempotent: the update predicate re-asserts both `status = "pending"` AND `expiresAt < now` so concurrent mutations between the candidate SELECT and the UPDATE don't get stomped. The status guard catches an athlete claiming/declining mid-sweep; the `expiresAt` guard catches an organizer extending a near-expiry invite via [[apps/wodsmith-start/src/server/competition-invites/issue.ts#reissueInvite]] — that path bumps `expiresAt` into the future while keeping `status = pending`, so a status-only re-check would let the sweep clobber the freshly-extended row's token. The `transitioned` count returned to the cron handler reads `rowsAffected` / `affectedRows` off the driver result rather than `ids.length` — when the re-check predicates filter out a candidate the actual transitioned count is what we want to log/alert on, not the optimistic SELECT size. Intended for hourly Cloudflare Cron Trigger wiring — Phase 2D ships the helper; the alchemy cron configuration lands with the round-builder in Phase 3 (when expiry semantics broaden to per-round deadlines).


## Auth route extensions for invites

Sign-in and sign-up accept `?email=<email>&invite=<token>` in addition to their existing search params. See [[apps/wodsmith-start/src/routes/_auth/sign-in.tsx]] and [[apps/wodsmith-start/src/routes/_auth/sign-up.tsx]].

When `invite` is present the email field is pre-filled from `email` and locked (disabled) so the user can't accidentally sign in / sign up with a different address — the whole point of the email-lock is that only this address can claim the invite.

We deliberately use `invite` rather than the existing `claim` param. `claim` is reserved for the KV-backed placeholder-user flow (see `/sign-up` + `validateClaimTokenFn` in [[apps/wodsmith-start/src/server-fns/auth-fns.ts]]), where the token is a plaintext KV entry pointing at an existing `users` row. Invite tokens live in DB (plaintext, mirroring `team_invitations.token`) and the user account may not exist yet, so the lookup target and post-auth behavior differ enough that collapsing both into one param would muddy both flows. Post-signup the user lands at the `redirect` param — always the original `/compete/$slug/claim/$token` URL — so the claim loader re-runs with the new session cookie and resolves to `identityMatch → ok`.

## Seed data

Running `pnpm db:seed` populates `competition_invite_sources` with a ready-to-demo scenario so the organizer `/invites` route renders non-empty. Phase 1 has no source-creation UI, so seeding is the only path to exercise the live tabs.

Scenario defined by [[apps/wodsmith-start/scripts/seed/seeders/20-competition-invites.ts]]: a championship competition "2026 WODsmith Invitational" (`comp_inv_championship`) receiving invites from three sources — a Regional Qualifier (`comp_inv_qualifier`) with 5 scored Men's RX athletes + allocation 3 that triggers the roster cutoff divider, Boise Throwdown (`comp_mwfc_a`) reusing MWFC series fixtures, and the MWFC series itself to demo the Series card + sub-tabs. The seeder sits after `19-broadcasts` and its tables are cleaned ahead of competitions in [[apps/wodsmith-start/scripts/seed/cleanup.ts]] (`competition_invites`, `competition_invite_source_division_allocations`, then `competition_invite_sources`).

ADR-0012 added per-(source, championship-division) allocation rows in `competition_invite_source_division_allocations`. The seeder writes them after the sources insert: the Qualifier and Boise sources each carry an explicit override for every championship division (mirroring the per-division `spots` that previously lived only inside the `division_mappings` JSON), and the MWFC series source carries a single override on Men's RX (`spots=4`, vs. the source default of `directSpotsPerComp(1) * compCount(1) + globalSpots(5) = 6`) — the other series divisions intentionally have no row so the source details page demos a mix of "Use default" and explicit-override toggles. Absence of a row signals "use the source default"; never seed `spots: 0` to mean "no override" — that's a hard cap of zero, not a default.

The championship and qualifier each carry two team-of-2 divisions in addition to the individual ones (`slvl_inv_champ_team_rx` / `..._team_sc` and the qualifier-side equivalents). The qualifier is seeded with three Team RX pairs (Thunder, Storm, Havoc) and two Team Scaled pairs (Grit, Pulse) — each as its own `competition_team` with owner=captain + member=teammate, both registered against the team division but only the captain holds a score. The qualifier source's `divisionMappings` covers both indy and team divisions (Team RX `spots=2`, Team Scaled `spots=1`), so the team-roster sub-views also show a cutoff divider. This setup feeds the captain-claim → team-registration form path: when the captain clicks their invite token, the championship registration form opens on a `team_size=2` division and collects the teammate.

The same seeder adds eleven Phase-2 `competition_invites` rows so every shape is inspectable in Drizzle Studio without manual editing. Rows 1–6 cover the individual divisions: pending source invite (Mike), `accepted_paid` (Ryan, activeMarker still "active", `claimToken` nulled), `expired` (Alex, activeMarker NULL), `declined` (Sarah), draft bespoke ("active but `claimToken = NULL`"), and sent bespoke (sponsored athlete). Rows 7–11 mirror those shapes against the team divisions: pending Team RX captain (Tyler), `accepted_paid` Team RX captain (Derek), pending Team Scaled captain (Ashley), `declined` Team Scaled captain (Stephanie), and a sent bespoke Team RX captain ("Sponsored team"). Live `claimToken`s are issued on the rows that need click-through testing — Mike (`seed-invite-mike-pending-men-rx-phase2`), the indy bespoke (`seed-invite-sponsor-bespoke-women-rx-phase2`), Tyler (`seed-invite-tyler-pending-team-rx-phase2`), Ashley (`seed-invite-ashley-pending-team-sc-phase2`), and the team bespoke (`seed-invite-sponsor-bespoke-team-rx-phase2`); all other rows leave `claimToken` NULL.
