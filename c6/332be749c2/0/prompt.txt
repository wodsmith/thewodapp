research the print-packet route in crew

---

the design of the page is horrible. I think we should have tabs for the different printable pieces. the main thing i want is a master schedule of events and their schedule times. then i want for each event a clear table of each heat and where each judge should be. then i want a tab for shifts that will print in clear sections the shifts and who is on what-when. there is a lot going on on that page that i dont think we need. lets come up with a plan to trim it down and focus the printable packets

---

flat time ordered list of shift sections

---

[Request interrupted by user]

---

implement the plan

---

Stop hook feedback:
The codebase has changes (1790 lines) but `lat.md/` may not be fully in sync (8 lines changed). Verify `lat.md/` is in sync — run `lat search` to find relevant sections. Run `lat check` at the end.

---

create a pr