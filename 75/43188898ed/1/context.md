# Session Context

## User Prompts

### Prompt 1

look at the changes in this branch. when i try to remove a judge rotation in the grid or in a published heat schedule, i get this error: These rotations are already attached to judge assignments. Publish a new judge schedule revision instead of replacing draft rotations.


we need to be able to update the published heat schedules by adjusting the grid or by directly editing the published version to create a new version.

### Prompt 2

Stop hook feedback:
The codebase has changes (734 lines) but `lat.md/` may not be fully in sync (12 lines changed). Verify `lat.md/` is in sync — run `lat search` to find relevant sections. Run `lat check` at the end.

