what reasons would the "transfer registration" button be disabled on the athlete details page? @apps/wodsmith-start/src/routes/compete/organizer/$competitionId/athletes/$registrationId.tsx

---

Stop hook feedback:
The codebase has changes (4584 lines) but `lat.md/` may not be fully in sync (26 lines changed). Verify `lat.md/` is in sync — run `lat search` to find relevant sections. Run `lat check` at the end.