the @apps/wodsmith-start/src/routes/compete/organizer/$competitionId/athletes/-components/event-submission-card.tsx doesn't respect multi-round workouts and naively shows 'reps completed at cap'. the workout in question has multiple rounds scored where timecapped reps are different for multi-round events. look at how this is handled on @apps/wodsmith-start/src/components/compete/video-submission-form.tsx

---

Stop hook feedback:
The codebase has changes (4263 lines) but `lat.md/` may not be fully in sync (26 lines changed). Verify `lat.md/` is in sync — run `lat search` to find relevant sections. Run `lat check` at the end.

---

lets use formatScore so it's displayed consistently everywhere

---

Stop hook feedback:
The codebase has changes (4578 lines) but `lat.md/` may not be fully in sync (26 lines changed). Verify `lat.md/` is in sync — run `lat search` to find relevant sections. Run `lat check` at the end.

---

why is a tiebreak showing for this score as `0.00.100` when its set to reps/weight and should just be a number? [Image #6]

---

[Image: source: /Users/zacjones/.claude/image-cache/dddbb65b-caa0-4710-b161-efd9dcd0e640/6.png]

---

Stop hook feedback:
The codebase has changes (4584 lines) but `lat.md/` may not be fully in sync (26 lines changed). Verify `lat.md/` is in sync — run `lat search` to find relevant sections. Run `lat check` at the end.