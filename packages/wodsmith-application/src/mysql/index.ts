/**
 * Server-only entry point for MySQL adapters.
 *
 * Domain slices export concrete adapters here. Client-safe code must import
 * from `@repo/wodsmith-application` or `/core`, never this subpath.
 */
export {}
